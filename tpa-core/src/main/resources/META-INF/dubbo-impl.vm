package ${table.packageName}.facade.impl;

##imports
import ${table.namespace};
import ${table.primaryKey.javaType};
import java.util.List;
import com.tongbanjie.commons.lang.Result;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import com.tbjfund.framework.tpa.OrderBy;

@Service
public interface ${table.beanName}FacadeImpl extends ${table.beanName}Facade{

    @Autowired
    private ${table.beanName}Service service;

    private Logger logger = LoggerFactory.getLogger(${table.beanName}FacadeImpl.class);

    Result<${table.primaryKey.SimpleJavaType}> getById(${table.primaryKey.SimpleJavaType} id){

        Result<${table.beanName}> result = null;
    }

    Result<List<${table.beanName}>> getList(${table.beanName} example, Integer offset, Integer rows, OrderBy orderBy);

    Result<List<${table.beanName}>> getList(${table.beanName} example, Integer offset, Integer rows);

    Result<Integer> getCount(${table.beanName} example);

    Result<Integer> insert(${table.beanName} example);

    Result<Integer> deleteById(${table.primaryKey.SimpleJavaType} key);

    Result<Integer> updateById(${table.beanName} example);

}